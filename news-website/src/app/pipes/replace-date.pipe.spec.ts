import { ReplaceDatePipe } from './replace-date.pipe';

describe('ReplaceDatePipe', () => {
  it('create an instance', () => {
    const pipe = new ReplaceDatePipe();
    expect(pipe).toBeTruthy();
  });
});
