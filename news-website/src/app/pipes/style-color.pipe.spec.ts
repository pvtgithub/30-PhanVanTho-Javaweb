import { StyleColorPipe } from './style-color.pipe';

describe('StyleColorPipe', () => {
  it('create an instance', () => {
    const pipe = new StyleColorPipe();
    expect(pipe).toBeTruthy();
  });
});
