import { SetFixedWhenScrollDirective } from './set-fixed-when-scroll.directive';

describe('SetFixedWhenScrollDirective', () => {
  it('should create an instance', () => {
    const directive = new SetFixedWhenScrollDirective();
    expect(directive).toBeTruthy();
  });
});
