import { StyleColorWhenHoverDirective } from './style-color-when-hover.directive';

describe('StyleColorWhenHoverDirective', () => {
  it('should create an instance', () => {
    const directive = new StyleColorWhenHoverDirective();
    expect(directive).toBeTruthy();
  });
});
